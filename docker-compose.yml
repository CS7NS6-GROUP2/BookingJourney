version: '3'
services:
  flask:
    build: JourneyBooking
    container_name: JourneyBooking
    restart: always
    environment:
      - APP_NAME=Journey
    expose:
      - 8080

  nginx:
    build: nginx
    container_name: nginx
    restart: always
    ports:
    - "80:80"